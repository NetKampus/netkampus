import CategoryTemplate from "@/components/categoryTemplate";

export default function GroupsPage({ searchParams }: any) {
  return <CategoryTemplate category="groups" searchParams={searchParams} />;
}
